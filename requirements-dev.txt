# Dependencias de desarrollo — no se instalan en producción.
#
# Instalar con:  pip install -r requirements-dev.txt
#
# NOTA: Los tests asumen que facebook_auto_poster/.env existe (copia de .env.example).
#       En CI, crear el .env antes de correr pytest.

pytest~=8.0
pytest-asyncio~=0.23
pytest-cov~=5.0
httpx~=0.27
tzdata~=2024.0
