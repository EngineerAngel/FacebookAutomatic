# Dependencies — pinned with ~= (compatible release, allows patch/minor updates
# but prevents major version breaking changes).
#
# Regenerar snapshot exacto con:  pip freeze > requirements.lock.txt
#
# Browser automation
patchright~=1.58
emunium~=3.0

# API + serving
flask~=3.0
waitress~=3.0
fastapi~=0.136
uvicorn~=0.46

# Config / networking
python-dotenv~=1.0
requests~=2.31

# Google Gemini SDK
google-genai~=0.3

# Cifrado de contrasenias individuales (Fernet / AES-128-CBC + HMAC)
cryptography~=42.0

# Logs estructurados en JSON (Fase 3.3a) — activos con STRUCTURED_LOGGING=1
structlog~=24.0

# HTTP async client (Fase 3.1 — HumanBrowsingAsync usa httpx en lugar de requests)
httpx~=0.27
